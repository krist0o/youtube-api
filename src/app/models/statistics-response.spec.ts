import { StatisticsResponse } from './statistics-response';

describe('StatisticsResponse', () => {
  it('should create an instance', () => {
    expect(new StatisticsResponse()).toBeTruthy();
  });
});
